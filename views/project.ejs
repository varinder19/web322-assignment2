<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= project.title %></title>
  <link href="/css/main.css" rel="stylesheet" />
</head>
<body>
  <%- include('partials/navbar') %>

  <main class="container mx-auto px-6 py-10">
    <section class="hero bg-base-200 rounded-2xl p-8 mb-8">
      <h1 class="text-4xl font-bold mb-2"><%= project.title %></h1>
      <p>Below you will find detailed information about the project.</p>
    </section>

    <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="rounded-xl mb-6 w-full max-h-96 object-cover" />
    <p class="mb-4"><%= project.intro_short %></p>
    <p class="mb-4"><strong>Impact:</strong> <%= project.impact %></p>
    <a href="<%= project.original_source_url %>" target="_blank" class="btn btn-outline mb-6">Original Source</a>

    <blockquote id="quote" class="italic p-4 bg-base-200 rounded-xl"></blockquote>
    <a href="#" onclick="history.back();return false;" class="btn btn-secondary mt-6">Back</a>
  </main>

  <script>
    document.addEventListener("DOMContentLoaded",()=> {
      fetch("https://dummyjson.com/quotes/random")
        .then(r=>r.json())
        .then(d=>document.getElementById("quote").textContent=`"${d.quote}" — ${d.author}`)
        .catch(()=>document.getElementById("quote").textContent="“Keep pushing forward.” — Unknown");
    });
  </script>
</body>
</html>
